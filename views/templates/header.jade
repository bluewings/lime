
	

.header
	.quick_menu
		div(ng-hide='status.selectMode', style='float:left')
			button.btn.btn-default.btn-sm(ng-click='modal.addNote()')
				span.glyphicon.glyphicon-plus
			

		div(ng-show='status.selectMode', style='float:left')
			span(ng-hide='status.selectedCount > 0')

				| 항목선택
			span(ng-show='status.selectedCount > 0')

				button.btn.btn-default.btn-sm(ng-click='modal.share()')
					span.glyphicon.glyphicon-share-alt

				button.btn.btn-default.btn-sm(ng-click='func.removeSelected()')
					span.glyphicon.glyphicon-trash

				| {{status.selectedCount}} 개가 선택됨

			




		.pull-right
			button.btn.btn-default.btn-sm(ng-click='func.toggleSelectMode()')
				| {{status.selectMode ? '취소' : '선택'}}
	.nav_bar
		//a.btn_toggle(ng-click="func.toggleHeader()")

			span.sim
				| 퀵메뉴 열고 닫기
		h2.title
			span
				| description
		.btn_group
			a.btn_toggle(ng-click="func.toggleHeader()")
				span.sim
					| 퀵메뉴 열고 닫기
	.nav_cover
		.cover
.cover
	h2
		| description

	button.btn.btn-default.btn-sm
		| bookmark
	button.btn.btn-default.btn-sm
		| archived
	button.btn.btn-default.btn-sm
		| create			

	button.btn_toggle(ng-click="func.toggle()")
		| add to homescreen




//.top(ng-class='status.open ? "on" : ""')
	.header
		.quick_menu
			button
				| Notes
			button
				| Archived




		.title_bar
			button.btn(ng-click="func.toggle()")
				| toggle
		
		.cover


	#cover



//.navbar.navbar-inverse.navbar-fixed-top(role='navigation')
	.container
		.navbar-header
			button.navbar-toggle(type='button', data-toggle='collapse', data-target='.navbar-collapse')
				span.sr-only Toggle navigation
				span.icon-bar
				span.icon-bar
				span.icon-bar
			a.navbar-brand(href='/home') QuickNote